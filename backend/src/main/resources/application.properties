spring.application.name=DashBoard-api

# ===============================
# SUPABASE POOLER
# ===============================

spring.datasource.url=${DB_PASSWORD}

spring.datasource.username=${DB_PASSWORD}

spring.datasource.password=${DB_PASSWORD}

spring.datasource.driver-class-name=${DB_PASSWORD}

# ===============================
# JPA / HIBERNATE
# ===============================

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true